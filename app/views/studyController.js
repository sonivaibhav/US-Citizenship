	// create the controller and inject Angular's $scope
	citizenshipPracticeApp.controller('studyController', function($scope) {
		// create a message to display in our view
		$scope.message = 'mainController.js!';
	});